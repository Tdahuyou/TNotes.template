---
id: e1e8122e-67b3-4136-960c-8b794100fe78
---

## 1. 本节内容

- `tn:update-completed-count` 去哪了

## 2. 评价

这条命令已经并进 `pnpm tn:update`（`tnotes-kb update`）。

不要再单独跑 `tnotes --update-completed-count`。统计字段现在在 `tnotes.json` 的 `stats.completedNotesCount`，不再写旧的 `.tnotes.json` → `root_item.completed_notes_count`。
